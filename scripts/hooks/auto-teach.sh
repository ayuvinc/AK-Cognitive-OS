#!/usr/bin/env bash
# auto-teach.sh — UserPromptSubmit hook (disabled 2026-04-28 — AK directive)
# Blocking gate removed: UserPromptSubmit hooks that block all responses are unacceptable.
# /teach-me remains available as a manual command. This hook is now a no-op.
exit 0
